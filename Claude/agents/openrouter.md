# OpenRouter

Living note for the **OpenRouter** agent (model: `openai/gpt-oss-120b:free`). Part of the [[index|triad]] roster — see [[layer 1 triad]]. Focus: [[cross-provider decorrelation]], [[generate-verify-select]].

## Log
- 2026-06-12 — joined the session; focus on [[cross-provider decorrelation]], [[generate-verify-select]].
- 2026-06-12 (parallel) — ## TL;DR **Generate‑Verify‑Select** is a three‑step “pipeline” for getting a *trustworthy* answer from a collection of LLM providers (or from the same provider with different prompts). 1. **Generate** – ask a set of LLMs (or the same model…
