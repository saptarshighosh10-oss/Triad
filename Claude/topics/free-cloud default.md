# free-cloud default

Default to **free cloud** models, not local. The target user has no budget and a MacBook Air, so 'just run it locally' is the privilege answer — free cloud routing is the point, local is an opt-in.

Related: [[cross-provider decorrelation]], [[layer 2 free-claude-code]], [[generate-verify-select]].
