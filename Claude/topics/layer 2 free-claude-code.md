# layer 2 free-claude-code

Separate tool: a proxy that drives a real file-editing coding agent on **free/local** models. Anthropic-shaped (`/v1/messages` on localhost:8082), so triad's Claude slot can run free through it via `TRIAD_CLAUDE_BASE_URL`.

Related: [[layer 1 triad]], [[layer 3 sidecar]].
