# 2026-06-12 gate-green-sandbox-built

GATE GREEN: Groq + OpenRouter stream through the real agent path (OpenRouter slug fixed to openai/gpt-oss-120b:free; NIM untested, no key). [[execution sandbox]] built + tested: macos-seatbelt tier active (real no-net floor, fs NOT isolated, Docker auto-used if present). Next: oracle.py + run_verify on top, with [[oracle independence]] enforced and oracle-absent = "unverified, selection only". See [[stage 2 verify-select]]. 91 offline tests green.

Back to [[index]].
