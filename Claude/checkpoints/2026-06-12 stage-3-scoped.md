# 2026-06-12 stage-3-scoped

SCOPED (not built): the last roadmap item — drive [[layer 2 free-claude-code]]'s **real file-editing agent** from triad. Verified locally how fcc works: `fcc-claude` spawns the actual `claude` Claude Code CLI as a subprocess with `ANTHROPIC_API_KEY` stripped + `ANTHROPIC_BASE_URL=http://127.0.0.1:8082` (the Anthropic-shaped fcc proxy, tier-routed to free models). So the bridge spawns a **CLI headless** (`claude -p`), not an SDK. Hard parts = isolation + gating + apply/discard, not the call. Plan: disposable `git worktree` (edit blast radius, one git command to undo) + reuse [[execution sandbox]] for the agent's bash/test runs + reuse verify-mode [[generate-verify-select]] oracle to gate the diff ([[oracle independence]] unchanged: no oracle → diff returned UNVERIFIED, never auto-applied). New modules: `triad/fcc.py` (proxy health + child env) and `triad/coder.py` (EditJob: worktree→headless claude→git diff→gate→apply/discard); wire `code` / `/code` / `/api/code`. Converges with agent-relay-terminal (same subprocess-driving primitive) — lean: EditJob lives in triad, relay calls it. Chat bridge (`TRIAD_CLAUDE_BASE_URL`) already works and is separate. Full scope: [[stage 3 — fcc file-editing bridge]]. NEXT, DO FIRST: confirm `claude -p` headless flags + stream-json stop signal (open q #1), then build `fcc.py`.

Back to [[index]].
